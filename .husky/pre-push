#!/usr/bin/env sh

echo "🔨 Type-checking service..."
if ! npx --no-install tsc --noEmit --incremental false --pretty false; then
  echo "❌ Type-check failed, aborting push."
  exit 1
fi

echo "✅ Pre-push checks passed."
